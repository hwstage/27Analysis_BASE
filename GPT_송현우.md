# Improving Language Understanding by Generative Pre-Training

> **핵심:** 라벨 없는 텍스트로 Transformer 언어 모델을 먼저 학습하고, 입력 형식과 출력층을 조금만 바꿔 다양한 언어 이해 과제에 fine-tuning한다.[^overview]

## 1. Introduction — 무엇을 해결하려는가?

자연어 이해 과제는 정답이 붙은 데이터가 부족하지만, 라벨 없는 텍스트는 풍부하다. 기존의 사전학습 단어 임베딩은 도움이 됐지만, **단어 수준을 넘어 문맥과 문장 관계까지 학습하고 전이하는 방법**은 명확하지 않았다.

논문의 해결 방향은 **언어 모델 사전학습 → 과제별 지도 fine-tuning**이다. 목표는 과제마다 복잡한 전용 모델을 만드는 대신, 하나의 범용적인 사전학습 모델을 최소한의 구조 변경으로 활용하는 것이다.[^intro]

## 2. Related Work — 기존 연구와의 차이

언어 모델을 먼저 학습한 뒤 특정 과제에 fine-tuning하는 접근은 이미 있었다. 따라서 이 논문의 초점은 사전학습 자체를 처음 제안하는 데 있지 않다.

기존 LSTM 기반 접근 대신 **Transformer로 긴 문맥을 처리**하고, 사전학습 표현 위에 복잡한 과제별 모델을 추가하는 대신 **단순한 입력 변환으로 더 다양한 과제에 적용**한다는 점이 핵심 차이다.[^related]

## 3. Framework — 어떻게 학습하는가?

### 3.1 Unsupervised pre-training

라벨 없는 텍스트에서 **앞선 토큰을 보고 다음 토큰을 예측**하도록 학습한다. 모델은 Transformer decoder를 사용한다.

이 단계에서는 특정 과제의 정답을 배우는 것이 아니라, 언어 모델링을 통해 이후 과제에 활용할 수 있는 표현을 학습한다.[^pretrain]

### 3.2 Supervised fine-tuning

과제의 입력을 사전학습 모델에 넣고, **마지막 위치의 hidden state에 선형 출력층을 붙여 정답을 예측**한다. 출력층만 학습하는 것이 아니라, 사전학습된 모델의 파라미터도 해당 과제에 맞게 조정한다.

과제 학습과 함께 언어 모델링을 보조 목표로 사용한다.

$$
L_3 = L_2 + \lambda L_1
$$

여기서 $L_2$는 **과제 정답 예측**, $L_1$은 **다음 토큰 예측**의 로그우도이며, 두 목표를 함께 최대화한다. 보조 언어 모델링은 일반화와 수렴을 돕기 위한 장치다.[^finetune]

### 3.3 Task-specific input transformations

과제마다 입력 구조가 다르므로, 이를 **모델이 읽을 수 있는 연속된 토큰 시퀀스**로 바꾼다. 논문의 **Figure 1(p.4)**이 이 과정을 보여준다.

| 과제 | 입력 처리 방식 |
|---|---|
| 문서 분류 | `[Start] 문서 [Extract]` |
| 문장 간 함의 판단 | `[Start] 전제 [Delim] 가설 [Extract]` |
| 문장 유사도 | `A → B`와 `B → A`를 각각 처리한 뒤, 두 최종 표현을 더해 예측 |
| 객관식 질문 답변·상식 추론 | 문맥·질문에 후보 답변을 하나씩 붙여 각각 처리하고, 후보별 점수를 softmax로 비교 |

`Delim`은 입력 요소를 구분하고, `Extract`는 과제 예측에 사용할 마지막 위치의 표현을 얻기 위한 토큰이다. **과제에 따라 크게 바뀌는 것은 모델 구조가 아니라 입력 구성 방식**이다.[^inputs]

## 4. Experiments — 실제로 효과가 있는가?

### 4.1 실험 설정

사전학습에는 7,000권 이상의 책으로 구성된 **BooksCorpus**를 사용한다. 문장이 따로 섞인 데이터와 달리 긴 연속 텍스트가 있어, 장거리 문맥을 학습하기에 적합하다는 점을 강조한다.

모델은 **12층 decoder-only Transformer**이며, 사전학습 시 길이 512의 토큰 시퀀스를 사용한다. 대부분의 과제는 fine-tuning 3 epoch로 학습했고, 보조 언어 모델링의 가중치는 $\lambda=0.5$로 설정했다.[^setup]

### 4.2 주요 결과

자연어 추론, 질문 답변, 문장 유사도, 문서 분류를 평가했으며, **12개 데이터셋 중 9개에서 논문 당시 최고 성능**을 달성했다.[^overall]

| 과제 | 핵심 결과 |
|---|---|
| 자연어 추론 | 5개 데이터셋 중 4개에서 기존 최고 성능 개선. MultiNLI matched 정확도 **82.1%**, 기존 대비 **+1.5%p**. 다만 RTE에서는 뒤처짐. |
| 질문 답변·상식 추론 | RACE **59.0%**, 기존 대비 **+5.7%p**. Story Cloze **86.5%**, **+8.9%p**. |
| 문장 유사도 | STS-B와 QQP에서 최고 성능. MRPC에서는 최고 성능에 미치지 못함. |
| 문서 분류 | CoLA 점수가 기존 **35.0 → 45.4**로 개선. SST-2는 **91.3%**로 최고 성능에는 미치지 못함. |

즉, 과제별 전용 구조 없이도 여러 종류의 언어 이해 문제에서 강한 성능을 보였지만, 모든 데이터셋에서 우세한 것은 아니다.[^results]

## 5. Analysis — 어떤 요소가 중요한가?

### 사전학습된 층을 얼마나 옮기는가?

임베딩만 가져오는 것보다 **더 많은 Transformer 층을 전이할수록 성능이 개선**됐다. 사전학습의 이점이 단어 임베딩에만 있는 것이 아니라, 상위 층에도 과제에 유용한 기능이 학습되어 있음을 시사한다.[^layers]

### Fine-tuning 없이도 능력이 생기는가?

언어 모델의 확률을 이용한 간단한 규칙으로, 지도 fine-tuning 없이 감정 분석·질문 답변 등을 평가했다. 사전학습이 진행될수록 이러한 **zero-shot 성능도 전반적으로 개선**됐다.

저자들은 이를 언어 모델링 과정에서 여러 과제에 필요한 기능이 일부 학습된다는 근거로 해석한다.[^zeroshot]

### Ablation

아래는 논문 **Table 5(p.8)**의 평균 점수다.

| 설정 | 평균 점수 |
|---|---:|
| 전체 모델 | 74.7 |
| Fine-tuning의 보조 언어 모델링 제거 | 75.0 |
| Transformer를 LSTM으로 대체 | 69.1 |
| 사전학습 제거 | 59.9 |

**사전학습의 효과가 가장 크고, Transformer 선택도 중요하다.** 반면 보조 언어 모델링은 NLI와 QQP 등에서 도움이 됐지만, 제거했을 때 평균 점수가 소폭 높았다. 따라서 모든 과제에서 이득을 주는 요소는 아니다.[^ablation]

## 6. Conclusion

**긴 연속 텍스트로 언어 모델을 사전학습하면, 그 모델의 지식을 구조 변경을 최소화하면서 다양한 언어 이해 과제로 전이할 수 있다.**

이 논문의 중심은 답변을 곧바로 생성하는 방식이 아니라, **생성형 언어 모델로 사전학습한 뒤 과제별로 fine-tuning하여 정답을 판별하는 방식**이다.[^conclusion]

