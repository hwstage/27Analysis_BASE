# DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning


> **핵심:** 사람이 작성한 풀이를 먼저 SFT로 학습시키지 않고도, **사전학습된 LLM에 강화학습을 직접 적용해 추론 능력을 높일 수 있음**을 보여준다. 여기에 SFT와 추가 RL을 결합해 가독성과 범용성을 개선한 모델이 **DeepSeek-R1**이다.

## 1. Introduction — 무엇을 해결하려는가?

기존의 추론 능력 향상 방법은 사람이 작성한 단계별 풀이, 즉 **Chain-of-Thought(CoT)**에 크게 의존한다. 저자들은 이러한 방식이 데이터 구축 비용이 크고, 모델의 탐색을 인간의 풀이 방식으로 제한할 수 있다고 본다.

논문의 출발점은 **“풀이 방법을 직접 가르치지 않고, 결과에 대한 보상만으로 더 좋은 추론을 유도할 수 있는가?”**이다. 이를 먼저 R1-Zero로 확인하고, 이후 실제 사용에 필요한 능력을 보완한 R1으로 확장한다.

## 2. DeepSeek-R1-Zero — SFT 없이 RL로 추론을 학습한다

**DeepSeek-V3-Base에 지도 미세조정(SFT) 없이 강화학습(RL)을 적용**한다. 여기서 SFT를 생략한다는 것은 사전학습까지 없다는 뜻은 아니다. 이미 사전학습된 모델을 출발점으로 사용한다.

### 2.1 Group Relative Policy Optimization

기존 알고리즘인 **GRPO**를 사용한다. 한 질문에 여러 답변을 생성하고, **같은 그룹 안에서 상대적으로 보상이 높은 답변을 더 잘 생성하도록** 학습한다.

각 답변의 상대적 성과인 *advantage*는 다음과 같이 계산한다.

\[
A_i = \frac{r_i - \operatorname{mean}(r_1,\ldots,r_G)}
{\operatorname{std}(r_1,\ldots,r_G)}
\]

즉, 그룹 평균보다 보상이 높으면 양수, 낮으면 음수가 된다. **별도의 가치 모델(value model) 없이 그룹 보상으로 계산**하므로, PPO보다 메모리와 연산 부담을 줄일 수 있다. (p. 15)

### 2.2 Reward Design

보상은 **정확도 보상 + 형식 보상**으로 구성한다.

| 보상 | 확인하는 내용 |
|---|---|
| **Accuracy reward** | 수학은 최종 정답이 맞는지, 코딩은 테스트 케이스를 통과하는지 확인 |
| **Format reward** | 추론 과정을 `<think>...</think>`처럼 지정된 형식으로 작성했는지 확인 |

핵심은 **중간 풀이의 내용을 단계별로 직접 채점하지 않는다는 것**이다. 추론 RL에는 학습된 신경망 보상 모델도 사용하지 않는다. 저자들은 그 이유로 보상 모델의 허점을 이용하는 **reward hacking**과 추가 학습 비용을 든다.

### 2.3 학습 중 나타난 변화

**Figure 1(p.4)**에서 AIME 2024의 pass@1은 **15.6% → 77.9%**로 상승한다. 학습이 진행되면서 응답 길이도 증가하고, 자신의 풀이를 검증하거나 다른 접근을 탐색하는 행동이 나타난다.

논문은 풀이 도중 멈춰서 기존 접근을 다시 검토하는 사례를 **“aha moment”**라고 부른다. 저자들의 해석은 **“반성·검증 절차를 정답 예시로 가르치지 않아도 RL 과정에서 이러한 행동이 나타날 수 있다”**는 것이다.

## 3. DeepSeek-R1 — 추론 능력에 가독성과 범용성을 더한다

R1-Zero는 추론 능력은 강하지만 **낮은 가독성, 언어 혼합, 일반적인 글쓰기·질문 답변의 한계**가 있다. R1은 이를 해결하기 위해 **SFT와 RL을 여러 단계로 결합**한다.

### 3.1 Model-based Rewards

정답을 규칙으로 판단하기 어려운 일반 과제에는 **학습된 보상 모델**을 사용한다. **Helpfulness는 최종 답변만**, **Harmlessness는 추론 과정과 최종 답변 전체**를 평가한다. 즉, 추론 과제와 일반 과제에 같은 보상 방식을 쓰는 것은 아니다. (pp. 6–7)

### 3.2 Training — 전체 학습 흐름

**Figure 2(p.6)**의 학습 흐름은 다음과 같다.

| 단계 | 하는 일 | 결과 모델 |
|---|---|---|
| **① Cold-start SFT** | 읽기 좋은 장문 CoT로 V3-Base를 미세조정 | Dev1 |
| **② 추론 중심 RL** | 정확도·형식 보상과 언어 일관성 보상으로 학습 | Dev2 |
| **③ Rejection sampling + SFT** | 생성한 답변 중 정답을 선별하고, 일반 과제 데이터와 함께 **V3-Base를 다시 SFT** | Dev3 |
| **④ 추가 RL** | 추론 과제와 일반 과제를 함께 학습해 추론 능력·유용성·안전성을 개선 | R1 |

Cold-start에는 **R1-Zero의 출력을 정제한 수천 개의 예시**를 활용한다. 이후 재학습용 데이터는 **추론 약 60만 개 + 비추론 약 20만 개 = 약 80만 개**로 구성한다.

언어 일관성 보상은 CoT에서 목표 언어가 차지하는 비율을 기준으로 한다. 가독성을 개선하지만, 일부 성능이 소폭 낮아지는 trade-off도 보고한다.

## 4. Experiment — 실제로 효과가 있는가?

### 4.1 학습 단계별 효과

**Table 3(p.9)**에서는 단계마다 개선하는 능력이 다르게 나타난다. Cold-start SFT는 지시 이행을 개선하지만 일부 추론 성능을 낮추기도 한다. 이후 **추론 중심 RL은 수학·코딩 성능을**, **일반 데이터와 선호도 학습은 지시 이행·응답 품질을** 주로 개선한다.

### 4.2 다른 모델과의 비교

아래는 **Table 8(p.41)**의 주요 결과다. 모두 pass@1(%)이며, **한 번 생성한 답변이 맞을 확률을 추정한 값**이다. 여러 답변 중 하나라도 맞으면 성공으로 치는 지표와는 구분된다.

| 벤치마크 | DeepSeek-V3 | o1-1217 | DeepSeek-R1 |
|---|---:|---:|---:|
| AIME 2024 — 수학 | 39.2 | 79.2 | **79.8** |
| MATH-500 — 수학 | 90.2 | 96.4 | **97.3** |
| LiveCodeBench — 코딩 | 36.2 | 63.4 | **65.9** |
| GPQA Diamond — 과학 추론 | 59.1 | **75.7** | 71.5 |

수학·코딩에서 o1-1217과 경쟁력 있는 성능을 보이지만, **모든 과제에서 더 좋은 것은 아니다**. 또한 o1-1217 수치는 저자들이 직접 재평가한 값이 아니라 공식 보고값을 인용한 것이다.

## 5. Ethics and Safety — 추론 능력과 안전성은 별개다

추론 능력 향상은 위험한 요청을 더 정교하게 수행하는 데도 이용될 수 있다. 논문은 탈옥 공격과 추가 fine-tuning에 따른 위험을 지적하며, **모델 자체의 안전성뿐 아니라 외부 위험 통제 시스템도 중요하다**고 강조한다.

## 6. Conclusion, Limitation, and Future Work

저자들이 강조하는 조건은 **강한 사전학습 모델 + 어려운 추론 문제 + 신뢰할 수 있는 정답 검증 + 충분한 RL 연산량**이다. 대규모 인간 풀이를 모방하는 것만이 아니라, **보상을 통해 더 좋은 풀이를 탐색하는 것**이 핵심이다.

남은 한계로는 **쉬운 문제에서도 과도하게 추론하는 비효율**, 도구 사용·구조화된 출력의 부족, 언어 혼합과 프롬프트 민감성이 있다. 특히 글쓰기처럼 정답 검증이 어려운 과제에서는 보상 모델의 허점을 이용할 수 있어, **추론 과제에서의 RL 성공을 모든 과제로 그대로 확장하기는 어렵다**.

