# LoRA: Low-Rank Adaptation of Large Language Models

> **핵심:** 사전학습 모델의 가중치는 고정하고, **과제 적응에 필요한 변화량만 작은 저랭크 행렬로 학습**한다. 학습 파라미터를 크게 줄이면서 full fine-tuning에 가까운 성능을 얻고, 학습 후 기존 가중치에 병합하면 추가 추론 지연도 없다.

## 1. Introduction — 무엇을 해결하려는가?

대규모 모델을 과제마다 **full fine-tuning**하면 모든 파라미터를 학습하고, 과제별로 거대한 모델을 따로 저장해야 한다. 모델이 커질수록 학습 메모리뿐 아니라 저장·배포·과제 전환 비용도 문제가 된다.

논문의 출발점은 **“모델 전체는 크더라도, 특정 과제에 적응하는 데 필요한 가중치 변화는 낮은 rank로 표현할 수 있지 않을까?”**라는 가설이다. LoRA는 기존 가중치를 그대로 두고, 이러한 작은 변화만 학습한다.

## 2. Problem Statement — 무엇을 다르게 학습하는가?

입력–정답 쌍을 이용해 과제 성능을 높이는 학습 목표는 full fine-tuning과 같다. 달라지는 것은 **최적화할 파라미터의 크기**다.

Full fine-tuning은 전체 모델 크기만큼의 변화량을 학습한다. 반면 LoRA는 변화량을 훨씬 작은 파라미터 집합으로 표현하고, **그 작은 파라미터만 최적화**한다.

## 3. Aren’t Existing Solutions Good Enough? — 기존 방법의 한계

**Adapter**는 기존 층 사이에 작은 모듈을 추가한다. 학습 파라미터는 적지만, 추가 모듈을 순차적으로 계산해야 하므로 **추론 지연이 발생**한다. 특히 작은 batch로 처리하는 온라인 추론에서 부담이 커진다.

**Prefix-tuning**은 학습 가능한 prefix를 이용한다. 논문은 최적화가 어렵고, prefix를 늘린다고 성능이 계속 좋아지지는 않는다고 보고한다. 또한 prefix가 시퀀스의 일부를 차지해 **실제 입력에 사용할 수 있는 길이가 줄어든다.**

## 4. Our Method — LoRA는 어떻게 작동하는가?

### 4.1 Low-rank update

기존 가중치 $W_0$를 고정하고, 업데이트를 두 작은 행렬의 곱 $\Delta W=BA$로 표현한다. 스케일링까지 포함한 출력은 다음과 같다.

$$
h = W_0x + \frac{\alpha}{r}BAx
$$

$W_0\in\mathbb{R}^{d\times k}$일 때, 학습하는 행렬은 $A\in\mathbb{R}^{r\times k}$와 $B\in\mathbb{R}^{d\times r}$다. $r$은 원래 차원보다 훨씬 작은 rank이고, $\alpha$는 업데이트의 크기를 조절하는 상수다. 따라서 해당 행렬의 학습 파라미터 수는 **$dk$에서 $r(d+k)$로 줄어든다.**

초기에는 **A를 무작위로, B를 0으로 설정**한다. 그러면 $BA=0$이므로 학습을 시작할 때는 기존 사전학습 모델과 같은 출력을 낸다.

학습이 끝나면 $W=W_0+\frac{\alpha}{r}BA$로 **가중치를 미리 병합**할 수 있다. 이후에는 일반적인 행렬 연산만 수행하므로, 병합된 모델에는 추가 추론 지연이 없다.

### 4.2 Transformer에 적용하기

원리상 다양한 가중치에 적용할 수 있지만, 논문은 **attention 가중치의 적응**에 집중한다. 대부분의 실험에서 **query와 value의 투영 행렬인 $W_q, W_v$**에 LoRA를 적용하고, MLP는 고정한다.

GPT-3 175B 실험에서는 학습 GPU 메모리를 **1.2TB → 350GB**로 줄였다. 또한 $W_q, W_v$에 $r=4$를 적용하면, 과제별로 저장할 가중치가 전체 모델 **350GB 대신 LoRA 가중치 약 35MB**가 된다.

단, **기본 모델 자체는 여전히 필요하다.** 35MB만으로 GPT-3를 실행하는 것이 아니라, 큰 기본 모델을 공유하고 과제마다 작은 LoRA 가중치를 추가하는 방식이다.

## 5. Empirical Experiments — 실제로 효과가 있는가?

### 5.1 RoBERTa·DeBERTa — 언어 이해

GLUE에서 학습 파라미터를 크게 줄이면서도 full fine-tuning과 비슷하거나 더 높은 평균 점수를 얻었다. 아래에서 M은 백만 개의 파라미터를 뜻한다.

| 모델 | 학습 파라미터: Full FT → LoRA | GLUE 평균: Full FT → LoRA |
|---|---:|---:|
| RoBERTa-base | 125M → **0.3M** | 86.4 → **87.2** |
| RoBERTa-large | 355M → **0.8M** | 88.9 → **89.0** |
| DeBERTa-XXL | 1,500M → **4.7M** | 91.1 → **91.3** |

### 5.2 GPT-2 — 자연어 생성

E2E NLG에서 GPT-2 Medium의 학습 파라미터는 **약 355M → 0.35M**으로 줄었지만, BLEU는 **68.2 → 70.4**로 높아졌다. 언어 이해뿐 아니라 생성 과제에서도 효율적인 적응이 가능함을 보여준다.

### 5.3 GPT-3 175B — 대규모 모델로 확장

WikiSQL은 자연어 질문을 SQL로 바꾸는 과제, MNLI는 문장 간 관계 판단, SAMSum은 대화 요약 과제다. 아래에서 WikiSQL·MNLI-m은 검증 정확도(%), SAMSum은 ROUGE-L이다.

| 방법 | 학습 파라미터 | WikiSQL | MNLI-m | SAMSum |
|---|---:|---:|---:|---:|
| Full fine-tuning | 약 175B | 73.8 | 89.5 | 44.5 |
| LoRA | 4.7M | 73.4 | **91.7** | **45.9** |
| LoRA | 37.7M | **74.0** | 91.6 | 45.1 |

설정에 따라 일부 점수는 full fine-tuning보다 낮지만, **매우 적은 학습 파라미터로도 대체로 동등하거나 더 좋은 성능**을 보인다. 파라미터를 더 많이 학습한다고 항상 성능이 높아지는 것도 아니다.

## 6. Related Works — 기존 연구와의 차이

저랭크 구조나 파라미터 효율적인 적응 자체를 처음 제안한 것은 아니다. 핵심은 **고정된 사전학습 모델의 ‘가중치 변화량’을 저랭크로 학습**한다는 점이다. 또한 Adapter와 달리, 선형적인 구조 덕분에 **학습 결과를 기존 가중치에 병합할 수 있다.**

## 7. Understanding the Low-Rank Updates — 왜 작은 rank로도 가능한가?

### 7.1 어느 가중치를 조정하는 것이 좋은가?

같은 학습 파라미터 예산에서 $W_q$나 $W_k$만 조정하는 것보다 **$W_q$와 $W_v$를 함께 조정하는 방식이 효과적**이었다. 한 종류의 행렬에 큰 rank를 사용하는 것보다, **여러 행렬에 작은 rank를 적용하는 편이 유리할 수 있다.**

### 7.2 Rank는 얼마나 커야 하는가?

GPT-3의 WikiSQL·MNLI 실험에서는 **$r=1$이나 $r=2$처럼 작은 rank로도 경쟁력 있는 성능**이 나왔다. 더 큰 rank가 항상 더 좋지는 않았다. 다만 저자들도 **모든 과제에서 작은 rank가 충분하다고 주장하지는 않는다.**

서로 다른 rank나 random seed로 학습한 결과를 비교하면, **주요 업데이트 방향 일부가 공통으로 나타난다.** 저자들은 이를 과제 적응에 중요한 방향이 소수일 수 있다는 근거로 해석한다.

### 7.3 기존 가중치와 업데이트는 어떤 관계인가?

업데이트 $\Delta W$는 기존 가중치 $W$의 가장 강한 방향을 그대로 반복하기보다, **기존 모델에 이미 있지만 상대적으로 덜 강조된 특징을 강화**하는 경향을 보였다.

저자들은 LoRA가 **사전학습에서 배운 특징 중 해당 과제에 중요한 것을 더 두드러지게 만드는 역할**을 할 수 있다고 해석한다.

## 8. Conclusion and Future Work 

**LoRA는 모델 전체를 작게 만드는 방법이 아니라, 과제에 맞게 바꿀 부분만 작게 학습하는 방법이다.** 이를 통해 큰 기본 모델을 공유하면서 과제별 학습·저장 비용을 줄이고, 가중치 병합 후에는 기존 모델과 같은 방식으로 추론할 수 있다.

한계도 있다. 가중치를 병합하면 **서로 다른 LoRA를 사용하는 과제들을 하나의 batch로 처리하기가 간단하지 않다.** 또한 어떤 가중치에 LoRA를 적용할지 체계적으로 결정하는 방법과, 적응이 작동하는 원리를 더 명확히 설명하는 것이 향후 과제로 남는다.
