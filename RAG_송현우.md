# Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks


> **핵심:** 사전학습된 검색기(DPR)와 생성기(BART)를 결합하고 함께 fine-tuning하여, **모델 파라미터에 저장된 지식과 외부 문서의 지식을 함께 활용**하는 생성 모델을 제안한다. 
## 1. Introduction — 무엇을 해결하려는가?

사전학습 언어 모델은 파라미터에 많은 지식을 저장하지만, **지식을 수정·추가하기 어렵고, 답변의 근거를 확인하기 어려우며, 사실과 다른 내용을 생성할 수 있다.** 외부 문서를 검색하는 메모리를 결합하면 이러한 한계를 줄일 수 있다는 것이 출발점이다. 
기존 REALM·ORQA 등이 주로 **문서에서 정답을 추출하는 QA**를 다뤘다면, 이 논문은 검색을 **범용적인 seq2seq 생성 모델**에 결합한다. 문서에서 답을 그대로 뽑는 데 그치지 않고, 검색한 정보를 활용해 답변을 생성하도록 한다. 
## 2. Methods — 검색과 생성을 어떻게 결합하는가?

**Figure 1(p.2)**의 전체 흐름은 **입력 → 관련 문서 검색 → 문서별 생성 확률 계산 → 확률 통합**이다. 검색 문서를 잠재변수로 두어, 어느 문서가 정답 생성에 기여하는지도 학습할 수 있게 한다. 
### 2.1 RAG-Sequence와 RAG-Token

두 모델 모두 top-K 문서를 검색하고, **문서별 생성 확률을 검색 확률로 가중합하는 주변화(marginalization)**를 사용한다. 차이는 이 통합을 **답변 전체 단위로 하는지, 토큰 단위로 하는지**다. 
| 모델 | 문서를 활용하는 방식 |
|---|---|
| **RAG-Sequence** | 각 문서 하나를 조건으로 **답변 전체의 확률**을 계산한 뒤, 문서별 확률을 통합 |
| **RAG-Token** | 각 문서를 조건으로 **다음 토큰의 확률**을 계산하고, 매 생성 단계에서 통합. 한 답변 안에서 여러 문서의 내용을 활용 가능 |

예를 들어 RAG-Sequence는 다음과 같이 표현한다. 여기서 $x$는 입력, $y$는 답변, $z$는 검색 문서다. 
$$
p_{\text{RAG-Sequence}}(y\mid x)
\approx
\sum_{z\in\mathrm{top}\text{-}K}
p_\eta(z\mid x)\,p_\theta(y\mid x,z)
$$

즉, **문서 하나만 선택하고 나머지를 버리는 방식은 아니다.** 또한 RAG-Token도 토큰마다 새로 검색하는 것이 아니라, **입력으로 검색해 둔 문서들의 생성 확률을 토큰마다 통합**한다. 
### 2.2 Retriever: DPR

질문과 문서를 각각 BERT 기반 인코더로 벡터화하고, **두 벡터의 내적이 큰 문서**를 검색한다. 이 Wikipedia 문서 인덱스가 모델 바깥에 있는 **비모수적 메모리(non-parametric memory)**다. 
### 2.3 Generator: BART

생성기는 사전학습된 **BART-large**다. **원래 입력과 검색 문서 하나를 연결**해 조건으로 제공하고, 답변을 생성한다. BART의 파라미터에 저장된 지식이 **모수적 메모리(parametric memory)**에 해당한다. 
### 2.4 Training

과제의 **입력–정답 쌍만으로 정답의 생성 확률을 높이도록** 검색기와 생성기를 함께 학습한다. 이 fine-tuning 단계에서는 어떤 문서를 검색해야 하는지 직접 정답으로 제공하지 않는다. 실제 업데이트 대상은 **질문 인코더와 BART**이며, 비용이 큰 **문서 인코더와 문서 인덱스는 고정**한다. 

다만 초기 검색기로 사용하는 DPR은 이미 NQ·TriviaQA의 검색 지도 신호로 학습되었다. 따라서 **전체 과정에서 검색 supervision이 전혀 없었다는 뜻은 아니다.** 
### 2.5 Decoding

RAG-Token은 통합된 다음 토큰 확률에 일반적인 beam search를 적용할 수 있다. RAG-Sequence는 **문서별로 답변 후보를 생성한 뒤, 여러 문서에서의 확률을 합쳐 비교**한다. 계산량을 줄이기 위한 근사 방식인 *Fast Decoding*도 제안한다. 
## 3. Experiments — 어떤 과제에 적용하는가?

외부 지식으로 **2018년 12월 Wikipedia**를 사용한다. 문서를 겹치지 않는 **100단어 단위로 나눠 약 2,100만 개의 passage**를 구성하고, 학습 시 질문마다 5개 또는 10개를 검색한다. 
| 과제 | 입력 → 출력 |
|---|---|
| **Open-domain QA** | 질문 → 짧은 정답. NQ, TriviaQA, WebQuestions, CuratedTrec에서 평가. |
| **Abstractive QA** | 질문 → 자연스러운 문장형 답변. MS-MARCO가 제공하는 정답 근거 문서는 쓰지 않고 직접 검색.  |
| **Jeopardy Question Generation** | 정답이 되는 개체 → 그 개체를 맞힐 수 있는 퀴즈의 설명·단서 생성. |
| **Fact Verification** | 주장 → 지지 / 반박 / 정보 부족. FEVER에서 분류 능력 평가. |

## 4. Results — 실제로 효과가 있는가?

### 4.1 Open-domain QA

**NQ·WebQuestions·CuratedTrec에서 논문 당시 최고 성능**을 달성했다. 아래는 **Table 1(p.6)**의 Exact Match 점수다. 
| 데이터셋 | DPR | RAG-Token | RAG-Sequence |
|---|---:|---:|---:|
| Natural Questions | 41.5 | 44.1 | **44.5** |
| TriviaQA — 표준 평가셋 | **57.9** | 55.2 | 56.8 |
| WebQuestions | 41.1 | **45.5** | 45.2 |
| CuratedTrec | 50.6 | 50.0 | **52.2** |

**TriviaQA에서는 평가셋을 구분해야 한다.** 표준 평가셋에서는 DPR보다 낮지만, T5와 비교하는 Wiki 평가셋에서는 RAG-Sequence가 **68.0점**으로 T5-11B+SSM의 **60.5점**을 넘어선다. 
또한 NQ에서 **검색 문서 어디에도 정답이 없는 경우에도 11.8%를 맞혔다.** 문서의 정답 구간만 추출하는 방식과 달리, 생성 모델의 지식도 활용할 수 있음을 보여준다. 
### 4.2 Abstractive QA

Open MS-MARCO에서 RAG-Sequence는 BART보다 **BLEU-1과 ROUGE-L 모두 2.6점 개선**됐다. 저자들은 생성 예시에서도 BART보다 사실과 다른 내용이 적고, 더 정확한 답변이 나타난다고 보고한다. 
### 4.3 Jeopardy Question Generation

이 과제에서는 **RAG-Token이 RAG-Sequence보다 좋은 성능**을 보인다. 인간 평가에서도 RAG-Token이 더 사실적이라고 판단된 경우는 **42.7%**, BART가 더 사실적이라고 판단된 경우는 **7.1%**였다. 이는 답변의 절대 정확도가 아니라 **두 모델 간 비교 결과**다. 
**Figure 2(p.7)**는 한 문장에서 서로 다른 작품명을 생성할 때 각 작품을 언급한 문서의 기여가 커지는 예를 보여준다. 저자들은 이를 **검색한 지식과 파라미터에 저장된 지식이 함께 작동하는 사례**로 해석한다. 
### 4.4 Fact Verification

FEVER 3-way 분류에서 **72.5%**를 기록해 BART의 **64.0%**보다 높았지만, 최고 성능 **76.8%**보다는 4.3%p 낮았다. 다만 RAG는 해당 과제의 근거 문서 라벨 없이 학습했다. 여기서 평가한 것은 **분류 라벨의 정확도이며, 근거 문장 추출까지 포함한 전체 FEVER 과제는 아니다.** 

### 4.5 Additional Results — 어떤 요소가 중요한가?

**생성 다양성과 검색 학습.** 생성 다양성은 RAG-Sequence가 가장 높고, 두 RAG 모델 모두 BART보다 높았다. 검색기를 고정하는 것보다 함께 학습하는 것이 전반적으로 유리했지만, **FEVER에서는 단어 겹침 기반 BM25 검색이 더 좋았다.** 
**지식 업데이트.** 모델을 다시 학습하지 않고 **Wikipedia 인덱스만 2016년판에서 2018년판으로 교체**해, 그 사이 바뀐 지도자에 대한 답변을 갱신할 수 있었다. 외부 메모리의 장점을 직접 보여주는 실험이다. 
**검색 문서 수.** 평가한 범위에서 문서 수를 늘릴수록 RAG-Sequence의 QA 성능은 좋아졌지만, RAG-Token은 10개 부근에서 최고 성능을 보였다. **더 많이 검색한다고 항상 더 좋아지는 것은 아니다.** 
## 5. Related Work — 기존 연구와의 차이

검색이나 외부 메모리 자체를 처음 제안한 논문은 아니다. 핵심 차이는 **사전학습된 검색기와 생성기를 결합하고, 같은 구조를 여러 지식 집약적 과제에 fine-tuning할 수 있게 했다는 것**이다. 과제마다 별도의 검색·추출 파이프라인을 설계하는 접근과 구별된다. 
## 6. Discussion — 기억할 핵심

**모델 내부의 지식과 외부 문서의 지식을 결합하고, 검색과 생성을 함께 학습하면 지식이 필요한 여러 과제에서 효과를 얻을 수 있다.** 특히 더 사실적인 생성과, 재학습 없는 지식 업데이트 가능성을 보여준다. 두 구성요소를 처음부터 함께 사전학습하는 방식은 향후 연구로 제시한다. 
### Broader Impact — 검색이 사실성을 보장하는 것은 아니다

외부 지식에 근거하면 환각을 줄일 수 있지만, **Wikipedia를 포함한 외부 지식 자체에도 오류와 편향이 있을 수 있다.** 검색을 결합했다고 답변의 정확성이 자동으로 보장되는 것은 아니다. 
